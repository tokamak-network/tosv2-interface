import useContract from "hooks/useContract";
import useUser from "hooks/useUser";
import { SupportedInputTokenTypes } from "types";

export function useTokenAllowance(
  token: SupportedInputTokenTypes,
  inputAmount: number
) {
  return null;
}
